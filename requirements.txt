# ENGRAM dependencies

PyYAML==6.0.3
numpy==2.5.2
sentence-transformers==6.0.0

# Natural Language Processing
nltk==3.10.3

# NLTK corpora/models are NOT pip packages. After installing the above, run:
#   python -m engram.nltk_data
# This downloads the required datasets (punkt, averaged_perceptron_tagger,
# maxent_ne_chunker, words, wordnet, omw-1.4, vader_lexicon) into the local,
# gitignored data/nltk_data directory so they are not fetched during runtime.

# spaCy provides the dependency parser used for relational fact extraction.
spacy==3.8.16

# The spaCy English model is a separate package (installed at setup, not at
# runtime). After installing the above, run:
#   python -m spacy download en_core_web_sm   # parser/tagger/NER/lemmatizer

confection==1.3.3
srsly==2.5.3
thinc==9.1.1
weasel==1.0.0


# Knowledge graph: MemGraph over the pymgclient driver (imports as `mgclient`).
# Matches the Tapestry knowledge-graph connection interface. Imported
# unconditionally like every other dependency (no lazy imports); the graph
# layer itself stays inert until graph.enabled is set in config.yml.
pymgclient==1.6.0

# Agent/LLM interface. Keep the stable v2 SDK line until MCP v3 is released.
mcp==2.0.0

# Single-instance RPC interface and standard health service. These pins match
# the deployed Tapestry/Engram runtime.
grpcio==1.83.0
grpcio-health-checking==1.83.0
protobuf==7.36.0


# Dependent Libraries

MarkupSafe==3.0.3
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
attrs==26.1.0
blis==1.3.3
catalogue==2.0.10
certifi==2026.7.22
cffi==2.1.1
charset_normalizer==3.5.1
click==8.4.2
cloudpathlib==0.25.0
colorama==0.4.6
cymem==2.0.13
cryptography==50.0.0
defusedxml==0.7.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
joblib==1.5.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
markdown-it-py==4.2.0
mcp-types==2.0.0
mdurl==0.1.2
murmurhash==1.0.15
networkx==3.6.1
opentelemetry-api==1.44.0
preshed==3.0.13
pycparser==3.0
pydantic==2.13.4
pydantic-core==2.48.0
pygments==2.21.0
pyjwt==2.13.0
pyopenssl==26.4.0
python-multipart==0.0.32
referencing==0.37.0
regex==2026.7.19
requests==2.34.2
rich==15.0.0
rpds-py==2026.6.3
shellingham==1.5.4
smart-open==8.0.1
spacy-legacy==3.0.12
spacy-loggers==1.0.5
tqdm==4.70.0
truststore==0.10.4
typer==0.27.1
typing-extensions==4.16.0
typing-inspection==0.4.4
tzdata==2026.3
urllib3==2.7.0
wasabi==1.1.3
wrapt==2.3.0
